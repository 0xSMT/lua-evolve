local Util = require "util"

local ranker = function(minimize)
    minimize = minimize or false  
    return function(population)
        -- NEED TO DO!!! tournament SUCKS BIG DICK
    end
end


return ranker